# Production Deployment Guide

## Docker Compose Deployment
```bash
docker-compose up --build -d
```

## Kubernetes Deployment
Deploy microservice images to K8s cluster using standard deployment manifests.
