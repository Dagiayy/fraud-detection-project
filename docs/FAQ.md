# Frequently Asked Questions

### Q: Why is SMOTE applied only on training splits?
A: To prevent synthetic data leakage into test evaluations.

### Q: How do I pass API Key headers?
A: Pass `X-API-Key: adey-fraud-secret-key-2025` in HTTP headers.
